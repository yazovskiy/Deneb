# Changelog

## 2.2.0 — 2026-09-10

- Общая справка CLI/F1 с отдельными страницами команд, вкладками и сокращением -h.
- Имя занимает половину таблицы; двухстрочное полное имя выбранного файла, адаптация к размеру терминала.
- «Убрать из списка», удаление только незавершённых данных и системная корзина готовых файлов на macOS — отдельные действия с подтверждениями.
- CLI remove --trash --yes; ошибки корзины сохраняют записи, сбой сохранения после переноса явно отмечается. Предварительную 2.2 с IPC v3 также нужно остановить перед обновлением.

- CLI: add (URL/stdin), list, show, pause/resume/remove по ID; JSON schemaVersion 1 и стабильные коды выхода.
- Поиск по имени и фильтры состояния; скрытые отметки сохраняются, массовые действия затрагивают только видимые.
- Ответ Add возвращает ID созданной задачи; ошибки добавления и потеря ответа не вызывают автоматический повтор.
- IPC v4, хранилище v5 без миграции; перед обновлением остановить старый фон.
- macOS ARM64 — стабильная сборка, Windows/Linux x64 — отдельный экспериментальный pre-release. 123 теста пройдены на трёх ОС; CLI, EN/RU PTY и SHA-256 проверены.

## 2.1.0 — 2026-09-08

- Общий лимит скорости в КиБ/с или МиБ/с с применением на лету; 0 — без ограничения.
- Общий учёт места для частей и сборки, запас 512 МиБ на том и ручное продолжение после нехватки места.
- Отдельная диагностика невозможности измерить место; проверки неизвестного размера по мере записи.
- Хранилище v5 с миграциями v1–v4; IPC v2 и совместимость только с фоном 2.1.
- EN/RU-интерфейс, проверки на подменяемых часах/дисках и приёмка на отдельном временном томе.
- macOS ARM64 — стабильный выпуск; Windows/Linux x64 — экспериментальные архивы.


## 2.0.0-experimental.1 — 2026-09-08

- Отдельные экспериментальные автономные сборки Windows x64 и Linux x64 (glibc); стабильный macOS 2.0.0 остаётся Latest.
- Архивы с явной пометкой experimental, ограничениями, лицензиями и SHA-256; упаковка и проверка на соответствующих ОС.
- Код приложения соответствует 2.0.0. Терминальный интерфейс Windows/Linux ещё не прошёл полную ручную приёмку; действия с файлами и буфер обмена пока только на macOS.
- Обновлены баннер и таблицы загрузок README на двух языках.

## 2.0.0 — 2026-09-07

- Загрузки продолжаются после закрытия интерфейса и терминала; единственный движок работает в отдельном фоне.
- Команды start/status/pause/resume/stop, остановка с подтверждением по F10, переподключение по F11/F12.
- Один интерфейс и параллельные CLI-команды; локальный версионированный IPC без публичного сетевого порта.
- Изменение максимума соединений на лету, безопасное разделение только недокачанных остатков и независимые сетевые повторы.
- Хранилище v4 со стабильными ID частей, миграцией v1/v2/v3 и отдельными резервными копиями.
- Переносимые ядро и управление процессом; CI-матрица macOS/Linux/Windows. Официальная сборка пока только macOS ARM64.
- Стабильный релиз только для macOS ARM64. Windows/Linux — экспериментальные платформы из исходников, без готовых бинарников.
- Автозапуск при входе и предотвращение сна не включены.

## 1.2.0 — 2026-09-07

- Полный английский и русский интерфейс, включая ошибки, подробности и CLI-справку.
- Английский по умолчанию для новых установок и обновлений; смена языка в F2 применяется без остановки загрузок.
- Независимые от языка коды фаз и ошибок; локализованное форматирование объёмов, скорости и ETA.
- Хранилище v3 с резервными копиями v1/v2 и преобразованием исторических ошибок.
- Английский и русский README, тесты ресурсов, миграций и живого переключения языка.
- Автономная сборка macOS ARM64; 45 автоматических тестов и PTY-проверки обоих языков из архива.

## 1.1.0 — первый публичный релиз

- Сохраняемый порядок, перемещение задач и «скачать следующей» без прерывания активных передач.
- Отметки строк, массовая пауза, продолжение и удаление.
- Общая пауза, сохраняемая после перезапуска и независимая от ручных пауз.
- Живые подробности сегментов и независимых повторов с обратным отсчётом.
- Скорость за последние пять секунд, ETA с поддержкой длительностей более суток.
- Открытие, показ в Finder, копирование пути и очистка списка без удаления файлов.
- Хранилище v2 с автоматической миграцией и отдельной резервной копией v1.
- 36 автоматических тестов и PTY smoke-test готовой сборки.
- Автономная сборка macOS ARM64, архив и SHA-256.

## 1.0.0 — локальная версия

- Полноэкранный интерфейс, добавление прямых URL и строк Android Download Manager.
- HTTP/HTTPS, очередь, сегменты, безопасная докачка и восстановление состояния.
- Атомарная публикация файлов, контроль валидаторов и защита от перезаписи.
